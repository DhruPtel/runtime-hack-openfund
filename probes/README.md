# Phase 0 probes

Throwaway scripts. Nothing under `src/` imports anything here.
Every probe writes its result to `research/findings.md` marked measured,
documented or inferred, with redacted bodies and a timestamp.
